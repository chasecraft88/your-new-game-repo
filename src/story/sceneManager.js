import * as THREE from 'three';

// Example of a simple scene manager
export const startScene = () => {
  // Initialize scene
  const scene = new THREE.Scene();
  // Set up your scene elements here
};
